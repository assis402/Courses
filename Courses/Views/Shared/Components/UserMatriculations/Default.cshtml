@model IEnumerable<Courses.Models.Matriculation>


<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-4">
            <div class="cards">
                <div class="card">
                    <img src="@Url.Content(item.CourseId)" class="card-img-top img-fluid" />
                    <div class="card-body">
                        <h5 class="card-title">@item.CourseId</h5>
                        <h5>Teste</h5>
                        <h5><span class="badge badge-success">R$ Teste</span></h5>
                    </div>
                </div>
            </div>
        </div>
    }

</div>